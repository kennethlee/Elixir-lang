# Etude 2-1: Write a module with a function that takes the length and width of a
# rectangle and returns (yields) its area.

defmodule Geom do
  def area(length, width) do
    length * width
  end
end
